import {Link, useNavigate, useLocation} from "react-router-dom";

export default function Navbar(){
    const {user}={user:{name: "John",email:"john@example.com", plan:"free"}};
    
    
    
    return (
        <div>
            <h1>Navbar</h1>
        </div>
    )   
}